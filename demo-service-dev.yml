server:
  port: 4001

eureka:
  instance:
    instance-id: demo-service-instance
    
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://170.20.10.5:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: refresh
