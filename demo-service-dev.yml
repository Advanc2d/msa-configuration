server:
  port: 40001

eureka:
  instance:
    instance-id: demo-service-instance
    
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://172.30.1.86:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: refresh
