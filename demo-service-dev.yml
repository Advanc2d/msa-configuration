server:
  port: 4002

eureka:
  instance:
    instance-id: demo-service-instance
    
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://192.168.219.101:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: refresh
