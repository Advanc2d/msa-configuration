server:
  port: 4001

eureka:
  instance:
    instance-id: auth-service-instance
    
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://172.20.10.5:8761/eureka
