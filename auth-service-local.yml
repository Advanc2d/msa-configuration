server:
  port: 4001

eureka:
  instance:
    instance-id: auth-service-instance
    
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://local:8761/eureka
